# Brazil UGC asset execution

Read `generation_packet` and the approved `ugc_draft`. Submit through the WaveSpeed API model `bytedance/seedance-2.0-mini/text-to-video`. The runtime fixes 15 seconds, 9:16, 480p, native audio, and one continuous take. Reference order and prompt labels are exact: `@image1` is the sole character identity, `@image2` is environment only, and optional `@image3` is the sole phone-screen game image. The phone must remain upright in portrait orientation, top edge up, with the screen front facing the lens during the display; optional `@image3` must appear directly inside that physical screen without redraw, replacement, or a newly generated UI. Keep local paths private. Reject an unexpected model, resolution, duration, or reference count before any paid request. The user's Generate click approves this exact draft and its references, never a different model or an additional attempt.

Persist `submitting` before the paid POST. Upload exactly the approved 2–3 reference files, then save the returned prediction ID immediately. On an ambiguous POST failure, report `submission_unknown` and do not retry. On restart, resume querying known prediction IDs. Download output before its provider URL expires. Return technical results honestly; face accuracy, hand physics, correct game-screen reproduction, spoken pt-BR timing, and lip sync still require checking the actual take. No multi-clip stitching, no invented replacement screen, and no automatic paid retry.
