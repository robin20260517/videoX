# Frontend implementation task

Implement studio-web/ only. Read ../brazil-ugc-spec.md (same docs directory) for exact API contracts and product requirements. Backend is owned by primary agent, do not edit it. User requested execution, do not stop for design approval. Do not spawn subagents. No commit necessary; primary coordinates integration.

Use React + Vite TypeScript Tailwind v4. Actual SmoothUI registry code must be fetched from https://smoothui.dev/r/<name>.json and installed, not hand-drawn. curl needs escalation if DNS blocked. Fetch with bounded --connect-timeout 15 --max-time 45. Keep source JSON under studio-web/vendor/smoothui/ and source attribution file. Use downloaded components: smooth-button, animated-file-upload, animated-input, animated-tabs or radio-group, animated-progress-bar/animated-stepper where meaningful. You can choose fewer if source dependencies warrant; all custom interactive control visuals must derive from official components. Official component source may be imported via a small installer using apply_patch to write files. No package changes elsewhere.

UI should be polished Chinese local workstation, grey-white editorial tokens from /home/robin/下载/灰黑网站设计提示词.md (read it). Product header videoX / 巴西 UGC 工作室. Main create panel: theme, one actor image (upload or default library), scene (upload or Pinterest board candidates), optional game screen image, pt-BR dialogue override, generate draft. Right production summary: 15s / 9:16 / 一镜到底; reference previews; explain roles. Draft review shows pt-BR line, timeline four continuous beats, sound/background/camera, expandable compiled prompt, button submit video. Jobs history and settings accessible through tabs/navigation. No fake videos, fabricated sample thumbnails, fake percentages or default fake characters.

Settings form saves API keys via backend PATCH, never browser localStorage. GET returns only booleans. Model fields public. Pinterest interface clearly calls it authorized Board and offers external search_url; imports clicked candidate then sets scene_id. Job polling GET lists; while queued/running call refresh every ~10s not overlapping. Prevent duplicate buttons during pending. On fetch errors show actionable message. Upload images multipart role/file max 10MiB one each; default character can be set via POST endpoint. On restart GET /jobs restores history.

Vite dev port 5173 proxies /api to http://127.0.0.1:8787. Production API serves dist. Add scripts dev, build, typecheck, test. Test important upload/form/failed request behavior with real components where possible. Report own files and build/tests in docs/frontend-report.md (only that doc outside studio-web permitted), state missing backend/live provider checks honestly.
